# Copy to .env and fill in real values for this deployment. Never commit
# the filled-in .env — only this example file (with placeholders) belongs
# in the repository. See docs/DEPLOYMENT.md for the full new-company
# deployment walkthrough.

# This deployment's own PostgreSQL database. Compass Tools is one
# application deployed once per company — each company gets its own
# dedicated database via this variable, never a shared database. Standard
# Prisma connection-string format.
DATABASE_URL="postgresql://user:password@host:5432/database_name?sslmode=require"

# Google Sign-In + "Connect Gmail" OAuth client (see
# src/server/auth/google-config.ts). A single Google Cloud OAuth app can be
# shared across every company's deployment — no separate Google Cloud
# project per company needed. TWO separate settings on that same OAuth
# client must both list this deployment's domain, or sign-in fails with
# "Error 401: invalid_client" / "no registered origin":
#   - Authorized JavaScript origins: https://<this-domain>  (origin only,
#     no path — required for Google Sign-In itself)
#   - Authorized redirect URIs: https://<this-domain>/api/auth/gmail/callback
#     (required only for the separate "Connect Gmail" flow)
# See docs/DEPLOYMENT.md §2a for the full walkthrough.
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# The Google account email that becomes this deployment's first Admin.
# ONLY has an effect while the database has ZERO Account rows — the very
# first time that exact person signs in with Google, an ACTIVE Admin
# account is created for them automatically; every other Google account
# signing in before that point is denied. Once ANY Account exists (whether
# created this way or otherwise), this variable permanently stops having
# any effect — it is never a standing admin-escalation mechanism, and the
# first Admin adds every other user by hand from the CRM's own /users page
# afterward. Compared case-insensitively, trimmed. Leave unset once the
# first Admin has been created — it no longer does anything, but an unused
# value here is not itself a security risk. See docs/DEPLOYMENT.md §2b.
INITIAL_ADMIN_EMAIL="your-name@example.com"

# Encryption key for Gmail OAuth refresh tokens at rest — AES-256-GCM,
# base64-encoded 32 random bytes. Generate a fresh, unique value per
# deployment (never reuse across companies/environments):
#   openssl rand -base64 32
GMAIL_TOKEN_ENCRYPTION_KEY=""

# This deployment's own public base URL — used to build secure
# customer-facing quote/booking links and email content, AND to derive the
# Gmail OAuth redirect_uri. On Vercel this can usually be left unset
# (falls back to VERCEL_PROJECT_PRODUCTION_URL, Vercel's STABLE production
# alias, then VERCEL_URL, a per-deployment URL that changes on every
# deploy); set explicitly for a custom domain or a non-Vercel deployment.
# Whichever origin ends up in use (this variable, or Vercel's own stable
# alias) is also exactly what must be registered as this OAuth client's
# Authorized JavaScript origin in Google Cloud Console — see
# docs/DEPLOYMENT.md §2a, including a real "Error 400: origin_mismatch"
# case study caused by registering an ephemeral per-deployment URL instead.
APP_BASE_URL="https://your-company-subdomain.example.com"

# Encryption key for stored card data — AES-256-GCM, base64-encoded 32
# random bytes. Generate a fresh, unique value per deployment:
#   openssl rand -base64 32
# NEVER reuse a value that appears in Git history or a test file, and keep an
# offline copy: without it stored cards cannot be recovered. This is NOT PCI
# DSS-grade key management — see docs/CARD_VAULT_SECURITY.md (key management,
# rotation, backups, remaining PCI DSS gaps).
CARD_ENCRYPTION_KEY=""

# Encryption keys for the IP capture vault (IpCapture — see
# src/server/security/ip-encryption.ts) — AES-256-GCM + HMAC-SHA256,
# base64-encoded 32 random bytes each. Generate two SEPARATE fresh values
# per deployment (never reuse across each other or across the keys above):
#   openssl rand -base64 32
IP_ENCRYPTION_KEY=""
IP_HASH_KEY=""

# Trusted reverse-proxy mode for client IP resolution (booking-signature
# audit trail — see src/lib/request-ip.ts). Leave UNSET for local
# development. On Vercel it is detected automatically (VERCEL=1) — nothing to
# set. Elsewhere set "cloudflare", "nginx" or "generic" to match whatever
# actually sits in front of this deployment, or "none" to opt out even on
# Vercel. See docs/DEPLOYMENT.md.
# TRUSTED_PROXY="generic"

# Pass 33 — DEPRECATED, no longer read by the application. Booking
# submission IP data is retained indefinitely by design; this variable
# used to gate the Reveal action's own authorization by age (never data
# deletion) and now has no effect at all. See src/server/actions/
# booking-security.ts and docs/DEPLOYMENT.md.
# BOOKING_IP_RETENTION_DAYS="90"  # deprecated — no longer has any effect

# Optional key ring for rotating CARD_ENCRYPTION_KEY without losing stored cards:
#   CARD_ENCRYPTION_KEYS="k2:<base64 32-byte key>,k1:<older key>"
#   CARD_ENCRYPTION_KEY_ID="k2"      # REQUIRED with CARD_ENCRYPTION_KEYS: the id new cards use
# CARD_ENCRYPTION_KEY itself stays valid as key id "v1". Keep every older key
# until no stored card or backup needs it. See docs/CARD_VAULT_SECURITY.md §6.

# In a production-class environment (production build / Vercel production or
# preview) the card vault stays CLOSED until the owner explicitly accepts
# application-level key management by setting exactly this value. It is not a
# boolean: "true", "staging" etc. do nothing. Not needed for local development.
# CARD_VAULT_MODE="application-encryption-risk-accepted"

# Database TLS verification. Unset = encrypted but the server's identity is NOT
# verified. For Aiven set the project CA certificate (PEM; a multi-line value
# or literal backslash-n sequences are both accepted):
# DATABASE_SSL_CA="<paste the PEM certificate here>"
# or, for a publicly trusted CA:  DATABASE_SSL_VERIFY="system"

# Scheduled purge of stored card numbers older than N days (daily cron). UNSET =
# never purge automatically — the retention period is a business decision. In
# production it only runs when CRON_SECRET is set.
# CARD_RETENTION_DAYS=""

# Only APP_ENV="production" has any effect (it forces production rules). Every
# other value is ignored and can NOT open the card vault or relax a production
# deployment. See docs/CARD_VAULT_SECURITY.md §5.
# APP_ENV="production"

# Bearer-token secret for the three /api/cron/* endpoints (leads, tasks,
# sequences) — see each route's own file comment. Left UNSET, those routes
# stay open with no auth check at all (fine for local dev, where nothing
# else can reach localhost). Once deployed anywhere public, set this to a
# random secret and configure your scheduler (Vercel Cron, etc.) to send it
# as `Authorization: Bearer <value>` — Vercel Cron does this automatically
# for any cron job when this env var is set on the project. Generate with:
#   openssl rand -base64 32
# CRON_SECRET=""

# Optional database connection tuning (per server instance) — see
# docs/DEPLOYMENT.md §5b. Defaults are sensible; set only if you have measured
# a need. Keep (instances x DATABASE_POOL_MAX) under your database's limit.
# DATABASE_POOL_MAX="2"
# DATABASE_POOL_CONNECT_TIMEOUT_MS="8000"
# DATABASE_POOL_IDLE_TIMEOUT_MS="4000"
# DATABASE_CONNECT_RETRIES="6"

# Only for a deliberate, brand-new environment: allows scripts/vercel-build.mjs
# to initialize a COMPLETELY EMPTY database. Leave unset otherwise.
# DATABASE_AUTO_INIT="true"
